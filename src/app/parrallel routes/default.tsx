/*
  Default content upon load of unmatched route
  Fall back when app can not retrieve active state of slot
*/

import React from 'react'

export default function Default() {
  return (
    <div>Default</div>
  )
}
