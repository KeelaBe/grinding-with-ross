/*
  This is parallel route, similar to a component
  Each parallel slot represents a small component for the individual route

  Useful for stateful sub naviagtion and independent error handling
*/
import React from 'react'

export default function Slot1() {
  return (
    <div>Slot1</div>
  )
}
